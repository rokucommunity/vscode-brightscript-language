import type { Options } from './StaticSiteGenerator';
import { StaticSiteGenerator } from './StaticSiteGenerator';
export declare const tempDir: string;
export declare const sourceDir: string;
export declare const outDir: string;
export declare const options: Options;
/**
 * Write a set of files to sourceDir
 */
export declare function writeFiles(files: Record<string, string>): void;
export declare function run(opts?: Options): Promise<StaticSiteGenerator>;
/**
 * Trim leading whitespace for every line (to make test writing cleaner).
 * Also removes empty lines for consistency
 */
export declare function trimLeading(text: string): string;
/**
 * Remove leading white space and remove excess indentation
 */
export declare function trim(strings: TemplateStringsArray, ...args: any[]): string;
