import { TextFile } from './TextFile';
import type { File } from '../interfaces';
export declare class HtmlFile extends TextFile {
    renderAsTemplate(file: File, content: string): string;
    publish(): void;
}
