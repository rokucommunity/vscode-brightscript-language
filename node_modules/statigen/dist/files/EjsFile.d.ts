import { TextFile } from './TextFile';
export declare class EjsFile extends TextFile {
    constructor(srcPath: string, outPath: string);
    renderAsTemplate(file: TextFile, content: string): string;
    publish(): void;
}
