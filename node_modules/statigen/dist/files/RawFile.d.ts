import type { Diagnostic, File } from '../interfaces';
export declare class RawFile implements File {
    srcPath: string;
    outPath: string;
    constructor(srcPath: string, outPath: string);
    diagnostics: Diagnostic[];
    publish(): void;
}
