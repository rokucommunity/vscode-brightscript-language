import * as vscode from 'vscode';
import { Extension } from './extension';

export class ExtensionWeb extends Extension {
    public async activate(context: vscode.ExtensionContext) {
        super.activate(context);
    }
}